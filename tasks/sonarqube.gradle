
apply plugin: 'org.sonarqube'

sonarqube {
    properties {
        property "sonar.projectName", "spring-boot-microservice-seed"
        property "sonar.projectKey", "spring-boot-microservice-seed"
        property "sonar.jacoco.reportPaths", "${buildDir}/jacoco/junitPlatformTest.exec"
    }
}
